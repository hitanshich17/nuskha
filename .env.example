# Copy to .env and fill in. Never commit .env.
POSTGRES_DB=skinvidhi
POSTGRES_USER=skinvidhi
POSTGRES_PASSWORD=change_me_locally

# Needed later for label reading and explanations
LLM_API_KEY=

# Only if your LocalStack image asks for one (free hobby tier)
LOCALSTACK_AUTH_TOKEN=
